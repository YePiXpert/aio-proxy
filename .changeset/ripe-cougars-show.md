---
'aio-proxy': patch
'@aio-proxy/server': patch
---

Show image, embedding, and audio models alongside language models in the OAuth Provider editor, including after refreshing the catalog. Hidden models remain available to re-enable.
